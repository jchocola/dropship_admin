import 'package:flutter/material.dart';

class CatalogInfoCard extends StatelessWidget {
  const CatalogInfoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(
        child: Text('Catalog Info Card'),
      ),
    );
  }
}