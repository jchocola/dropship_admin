import 'package:flutter/material.dart';

class PickedOrderDetailWidget extends StatelessWidget {
  const PickedOrderDetailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(child: Text('Picked Order Detail'),),
    );
  }
}