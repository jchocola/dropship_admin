import 'package:flutter/material.dart';

class DiscountListWidget extends StatelessWidget {
  const DiscountListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(
        child: Text('Discount List'),
      ),
    );
  }
}