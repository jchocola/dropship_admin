import 'package:flutter/material.dart';

class CurrentShopPage extends StatelessWidget {
  const CurrentShopPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Current Shop Page')));
  }
}
