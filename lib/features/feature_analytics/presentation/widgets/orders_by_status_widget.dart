import 'package:flutter/material.dart';

class OrdersByStatusWidget extends StatelessWidget {
  const OrdersByStatusWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(child: Text('Order by Status')),
    );
  }
}